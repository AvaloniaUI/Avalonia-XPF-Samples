<UserControl x:Class="HybridXPF.Views.WpfContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Margin="4" Padding="4" Background="#C0C0C0" BorderBrush="Brown" BorderThickness="4">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="Standard WPF Controls" Foreground="Red" />
            <TextBox Text="{Binding Greeting, Mode=OneWay}" DockPanel.Dock="Top" />
            <Calendar />
        </DockPanel>
    
    </Border>
</UserControl>

